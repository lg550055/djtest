# Test project to try django unit tests

- Pages uses the SimpleTestCase class to build unit tests that do not access the DB

- Posts uses the TestCase class to test DB-related and non-related functionality

---